#ifndef H_MENU
#define H_MENU
#include <SFML/Graphics.hpp>
#include <iostream>
#include <random>
#include <time.h>

using namespace sf;
void menu(RenderWindow& window);
extern int game(RenderWindow& window);
#endif
